-- Task 1: Select all employees
SELECT *
FROM employees;

-- Task 2: Select specific columns (first name, last name, department)
SELECT first_name, last_name, department
FROM employees;

-- Task 3: Select employees with salary and hire date
SELECT first_name, last_name, salary, hire_date
FROM employees;
